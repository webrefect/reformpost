services:
  - type: web
    name: reformpost
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    preDeployCommand: rm -rf /src node_modules package-lock.json 2>/dev/null || true    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: NODE_OPTIONS
        value: --experimental-sqlite
